import {increment, INCREMENT, decrement, DECREMENT} from './actions';

describe('increment', () => {
    it('Should return the action');
});

describe('decrement', () => {
    it('Should return the action');
});
